[pytest]
asyncio_mode = auto
markers =
    security: security validation tests
    performance: performance benchmark tests
    slow: long-running tests
    unit: fast isolated tests (registered so -m filters don't warn)
